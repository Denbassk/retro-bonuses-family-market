# CLAUDE.md — ядро памяти проекта «Ретро-бонусы Family Market»

> Только правила и текущее состояние. Детали и история — в `docs/`, карта `docs/INDEX.md`.
> Полная прежняя память — `docs/archive/`. State.md читать НЕ обязательно:
> он про архитектуру, открывать по необходимости.
> Если docs/ противоречит этому файлу — прав этот файл.

## Работа

PowerShell на Windows, `D:\РЕТРО_БОНУСЫ Фэмэли маркет`. Правки файлов — here-string
с `Set-Content -Encoding UTF8`, после каждого шага команда коммита. Репозиторий приватный.
Конвейер — `core\`, запуск из корня: `python core\<скрипт>.py`. Генерируемое — в `output\`
(через `core\paths.py`). Перед любым вопросом по правилам читать `output\rules_snapshot.md`
(`python core\dump_rules.py`) — не спрашивать то, что уже есть в БД.

## Цель

Загрузил Excel -> разложено по поставщикам и месяцам -> сверено с расчётом ->
совпало: записано тихо; разошлось: диагноз с числами.

## Где мы (на 2026-09-13)

1. Парсер Excel — ГОТОВО (10 контрольных сумм сходятся).
2. Маппинг имён — ЗАКРЫТО (`load_name_map.py --apply`, 49/49, автоугадывания нет).
3. Запись фактов — ГОТОВО (`retro_payments_fact`, 304 строки, идемпотентно).
4. Автосверка — код готов, сухой прогон OK. ЖДЁТ SQL, затем `core\reconcile_facts.py --apply`.
5. Диагностика — код готов (`core\diagnose_retro.py`), запускается из сверки.

Ждут выполнения в SQL Editor: `sql\2026-09-11_reconciliation.sql`,
`sql\2026-09-11_import_journal.sql`, `sql\2026-09-11_adjustments_payment_mode.sql`
(после последнего — `tools\migrate_adjustment_modes.py --apply`, потом reconcile --apply).

## Инварианты: Excel фактов

- Актуальная выгрузка — в `Ретро_Excel\`. Копия в корне устарела, не читать.
- Шапка в строке 1, данные со строки 2. Парсить ТОЛЬКО по имени заголовка:
  между месяцами вклинены колонки разовых бонусов.
- «Общая сумма» — контрольная строка, не поставщик. Суммы не сошлись — запись запрещена.
- `None` (не заполнено) и `0` (платить нечего) — разные состояния. NULL молча пропускать,
  0 сверять с расчётом 0.
- Суммы округлены до гривны -> порог допуска >= 1 грн.
- Колонки-адреса (роган.148, зерновая 6/5, Семенка 17, Неск.4д, за нов.маг.) — бонусы
  на открытие магазина, не ретро, в сверку не входят -> `store_opening_bonuses`,
  ключ (excel_name, store_label, year).
- Примечание `ASISTEC: 12.03.26` = автор и дата, когда принесли деньги. Если число в
  примечании РАВНО сумме ячейки — это заявленный итог, а не слагаемое.
- Колонка «Условия» — заметка человека. Источник истины `retro_rules`; использовать
  только как сигнал расхождения ставки.

## Инварианты: сверка и расчёт

- Ретро считается по ПОЛНОМУ закрытому месяцу. Текущий месяц из сверок исключать.
  Рабочее окно — 2026-01 .. последний закрытый.
- TRUTH_UNTIL = 2026-06. Январь-июнь НЕ ПЕРЕСЧИТЫВАТЬ: истина — сохранённый расчёт
  (проверено числами: пересчёт на перезалитых данных уводит от факта). Июль+ пересчитывать.
- Допуски: <= 1 грн MATCH, <= 2% MINOR, выше MISMATCH. Пустой факт <= 2 мес. после
  периода = PENDING (95% оплат приходят за 2 мес.), позже NOT_PAID.
- Разнознаковые расхождения, гасящиеся за окно, — не ошибка: поставщик заплатил за
  2-3 месяца одной суммой, админка разнесла. Сравнивать нарастающим итогом
  (REDISTRIBUTION). Разноска в админке правильнее, Excel её не затирает.
- Сверять на агрегате поставщик-месяц, при расхождении спускаться в бренды.
  Excel = одна сумма на месяц, расчёт = строки по брендам.
- Админка — источник истины для внесённого, Excel — для нового. Конфликты не
  перезаписываются без `--with-conflicts`, прежнее значение -> `amount_previous`.
- БІР Кег + Славутич сверять группой. Союз (Оболонь) = EXCLUDED_OWNER, не трогаем.
- НДС: «минус 20%» = x0.80 для всех `subtract_vat_from_retro`. Хардкод убран,
  логика в `apply_vat()`. Регрессия: Виналь 07 = 26 959,82.

## Инварианты: чтение BigQuery

- Дубли проверять ТОЛЬКО по `line_id`. `incoming_id + line_number` — НЕ ключ
  (одна строка документа лежит в разных store/barcode). Дублей нет, проверено.
- `loaded_at` — DATETIME по КИЕВСКОМУ времени, не UTC. Игнор пояса даёт ложные STALE.
- Эталон `torgsoft_incoming_ref_2026` хранит поставку ДВАЖДЫ (короткий внутренний и
  длинный внешний номер). Несовпавшие документы гасить парой (дата+сумма), остаток — дыра.
- В эталонах НЕТ barcode -> сверка только агрегатом поставщик x месяц, без SKU-фильтров.
  Это контроль СЛОЯ ДАННЫХ (алиас/склад/документ), а не слоя правил.
- Окна эталонов: приходы по 31.08, возвраты продлены по 31.08. Вне окна «эталон 0» —
  граница выгрузки, не расхождение.
- Возвраты: база «всё подряд». `is_internal` пустой, в модель не тащить.
- Никогда `SELECT *`. Агрегировать в SQL, всегда указывать окно дат.
- Слой данных СВЕРЕН И ПРИНЯТ: в ретро-скоупе валовая дельта 477 тыс. (0,64%)
  = около 62 тыс. грн ретро; возвраты 4 253 грн (около 411 грн). Тема закрыта,
  не пересверять. Прежние оценки 180 тыс. / 36 тыс. / 200+ тыс. ошибочны и удалены.

## СТОП-правила

- Резолв имён: либо EXACT, либо человек. Никакого FIRST_WORD, никакого матчинга по сумме.
- Совпадение «номер + дата + сумма» у двух имён означает только, что документ один.
  Это НЕ основание для алиаса. Алиас — только доказанно одно юрлицо, один бренд, одна
  ставка. Разные юрлица со схожими именами ЗАВОДИТЬ отдельно:
  Хардпрод != Хладопром, Білосвіт != Бісквіт і Ко, Кріофудтрейд != Айсмейстер,
  Ново-Баварський != Юрія, СТВ (Корона) != СТВ (Каваапарат).
- Не ретро — не заводить и не алиасить: ПП ТД СМК Груп, Айсмейстер, Інтрейд Мікс (Чай),
  Авангард Дистрибуції (продукти), Грілайн ЛТД, Геотрейд-ЮА.
  Авангард Дистрибуції (ваговий) — алиас excluded, ретро нет.
- Не ожидается в Excel (ретро по безналу, статус NOT_EXPECTED): Ново-Баварський,
  Аванта-Трейд (2 направления), Еліт Фуд, Сервіс Про с 05. `bonus_form` НЕ признак
  безнала: у Жирнова marketing_service, но наличка и строка в Excel есть.
- Перед выводом «деньги потеряны» фильтровать по `retro_base_type` / `income_source`:
  payments, per_portion_sold и torgsoft_ref не реагируют на дыры в
  `incoming_transactions`. Пропуск шага завышает оценку примерно вдвое.

## Правила изменений

- Секреты только в `.env`; данные (`*.xlsx`, `*.csv`, `payments\`, `output\`,
  `credentials\`) — в `.gitignore`.
- Скрипты не пишут в БД, пока вывод не проверен глазами на CSV.
- Корректировки — только через `retro_adjustments`, НЕ правкой
  `retro_calculation_details`. Режимы: `in_payment` (входит в total_retro строкой
  расчёта) и `separate` (только запись; при сравнении прибавляется к факту).
- Доступ к Supabase — только через `core\sb.py` (пагинация, батчи). Перед правкой
  общего хелпера найти все места вызова ПО СИМВОЛАМ, не текстовым поиском.
- Админку раздаёт `core\admin_server.py` (127.0.0.1:3000, токен на каждый POST).
  Старый http.server отдавал всю папку вместе с `.env` — не использовать.

## Открытые вопросы владельца

- SPLIT «Бир Компани (Славутич пиво)»: 50 000 бонусов на открытие (роган.148 и
  зерновая 6/5 по 25 000) не записаны — плательщика выбирает человек.
- `is_prosrok`: уменьшает ли просрочка базу ретро. Снимает 592 тыс./год, сейчас
  просрочка в наши данные ВКЛЮЧЕНА. Вопрос слоя правил.
- Прогон по мультибрендовым (Союз, СТВ, Арсенал, Глобал-Сервіс, БІР) на ошибку
  привязки бренда: подтверждён 1 случай (Оболонь 533,64 от 08.08 ушёл в Наш Сік).
- Паттерны диагностики, требующие решения по правилам: Роганський МК и ФКК Авангард
  платят ретро и на «сырьё»; Монжар 05-06 на исключённые Юмі Джелі; Гранд Дистрибьюшн,
  Азнаурі 05, Корма 05, СТВ Наша Ряба 06 не вычитают возвраты; Бісквіт і Ко 6,2% вместо 6%.

## Бэклог кода

1. Защита периодов `approved`/`paid` от перезаписи + флаг `--force`.
2. Явный флаг `is_raw` вместо regexp `^сырье` по product_name.
3. Дрейф округления при распределении возвратов -> метод наибольшего остатка.
4. Регрессионные тесты на сверенных копейка-в-копейку эталонах.
5. Источник оплат для `retro_base_type=payments` (7 поставщиков: АСК, Мон Шер,
   Хладопром, Дісна, Баядера, Продресурс, Рома) — сейчас только банковская выписка
   в `supplier_payments`; пустой месяц выписки = расчёт 0 = ложное «не оплачено».

## Где что искать

- `docs/INDEX.md` — карта документов.
- `docs/reference/architecture.md` — слои, таблицы, логика расчёта.
- `docs/reference/suppliers.md` — особенности ключевых поставщиков.
- `docs/reference/bq-etalons.md` — эталоны Торгсофт, окна, метод сверки.
- `docs/archive/State-2026-09-13.md` — SQL-шпаргалки BigQuery и Supabase (разделы 10-11).
- `docs/archive/CLAUDE-2026-09-13.md` — вся история диагнозов и прогонов.

# CLAUDE.md — память проекта «Ретро-бонусы Family Market»

> Читать вместе с `State.md` (описание архитектуры и БД).
> Этот файл — рабочий контекст: где мы, куда идём, что знаем о данных.

## Кто с кем работает

Пользователь работает в **PowerShell на Windows**, папка `D:\РЕТРО_БОНУСЫ Фэмэли маркет`.
Договорённость: файлы создаются/правятся через PowerShell (`@'...'@ | Set-Content -Encoding UTF8`),
после каждого шага — команда коммита. Репозиторий `Denbassk/retro-bonuses-family-market`,
**приватный** (временно открывался для первичного разбора).

## Задача, которую решаем

Убрать ручной ввод фактов оплат ретро. Сейчас: руками вносятся суммы в админку,
глазами ищутся расхождения с расчётом, руками пишутся SQL в BigQuery.
Цель: загрузил Excel → система разложила факты по поставщикам и месяцам →
сверила с расчётом → при совпадении тихо записала, при расхождении выдала
диагностику с числами.

## Этапы (v1)

1. **Парсер Excel без записи в БД** — `import_retro_facts.py`. ГОТОВО.
2. **Резолв имён** через таблицу `retro_fact_name_map`. Только EXACT автоматом,
   остальное — на ручное подтверждение. ← текущий этап
3. **Запись фактов** в `retro_payments_fact` (upsert, с сохранением предыдущего значения).
4. **Автосверка** → таблица `retro_reconciliation` (два порога: абсолютный и %).
5. **Диагностика расхождений** — детерминированный набор проверок-гипотез.

## Что знаем о факт-таблице Excel («Ретро Бонусы.xlsx», лист «2026»)

- **Шапка в строке 1**, данные со строки 2. Автодетекторы ошибочно считают шапкой строку 2.
- Колонка A — поставщик, B — «Условия» (смесь `0.15` и текста `от 9,5-15%`).
- Дальше колонки месяцев, но **между ними вклинены разовые бонусы**:
  2026 — `роган.148` (D), `зерновая 6/5` (I); 2025 — `за нов.маг.`, `Семенка 17`, `Неск.4д`.
  Поэтому парсить **только по имени заголовка**, никогда по позиции.
- Внутри данных есть строка **«Общая сумма»** — контрольная сумма, не поставщик.
- `None` (не заполнено) и `0` (нечего платить) — **разные состояния**, не схлопывать.
- Суммы округлены до гривны → нижний порог допуска сверки ≥ 1 ₴.
- Имена плавают между годами: `Арсенал (Азнаурі)` (2025) vs `Арсенал (Азнаури)` (2026),
  хаотичные пробелы. Нормализация: регистр, і/ї/є/ґ → и/и/е/г, схлопывание пробелов, скобки.

### Прогон парсера 2026 (эталон, 49 поставщиков)

Все 10 контрольных сумм сошлись, дублей нет. Ячейки: 300 значений, 39 нулей, 347 пустых.

| Период | Итог |
|---|---|
| 2026-01 | 1 150 513 |
| 2026-02 | 1 098 112 |
| 2026-03 | 1 439 337 |
| 2026-04 | 2 073 693 |
| 2026-05 | 1 984 958 |
| 2026-06 | 1 960 764 |
| 2026-07 | 1 699 930 |
| 2026-08 | 41 309 (месяц в работе) |
| зерновая 6/5 | 39 508 |
| роган.148 | 32 500 |

## Известные проблемы (в порядке важности)

1. **FIRST_WORD-матчинг опасен.** В `payment_facts_mapping.template.json` «Союз (Наш Сок)»
   привязан к «Союз (Жако…)», «СТВ Схид (Якобз)» — к «СТВ Схід (Корона…)», «Арсенал (Моршин)» —
   к «Арсенал ПК (Азнаурі)». У поставщика 3–5 направлений, все сматчились на первое по алфавиту.
   **Правило: либо EXACT, либо человек. Никакого угадывания.**
2. **`VAT_FACTOR` объявлен, но не используется** — в `calculate_retro.py` в 4 местах хардкод
   `Decimal("0.80")`. Менять константу бесполезно. Вынести в `apply_vat()`.
   Отдельно решить: ×0.8 («минус 20%») или ÷1.2 («выделить НДС») — разница 4,2%.
3. **Ручные правки затираются пересчётом.** Нужна таблица `retro_adjustments`
   (накладывается поверх расчёта), а не правка `retro_calculation_details`.
4. **Утверждённые периоды не защищены** — статус `approved`/`paid` не мешает перезаписи.
   Нужен пропуск + флаг `--force`.
5. **Дрейф округления** при пропорциональном распределении возвратов (вариант B).
   Лечится методом наибольшего остатка: остаток — последнему правилу.
6. **`sb_get`/`sb_post`/`load_env` скопированы в 8 файлов** в разных версиях; автопагинация
   есть только в `calculate_retro.py` → остальные молча теряют данные при >1000 строк.
7. **Импорт по одной строке** — файл АСК идёт 5–10 мин. Нужен батч по 500.
8. **Нет регрессионных тестов**, хотя 8 поставщиков сверены копейка в копейку — готовые эталоны.

## Открытые вопросы

- Что такое `зерновая 6/5` — разовый бонус за открытие магазина сверх ретро или иное?
- Смысл `subtract_vat_from_retro` (см. п.2 выше).

## Конвенции

- Секреты только в `.env` (в Git не идёт, есть `.env.example`).
- Данные (`*.xlsx`, `*.csv`, `payments/`, `output/`, `credentials/`) — в `.gitignore`.
- Скрипты не пишут в БД, пока их вывод не проверен глазами на CSV.

## Разовые бонусы за открытие магазина (уточнено)

Колонки вида `роган.148`, `зерновая 6/5`, `Семенка 17`, `Неск.4д`, `за нов.маг.` -
это **оплата поставщика на открытие нового магазина**, столбец = адрес точки.
НЕ ретро за период: расчётного аналога нет, в сверку не входят.
Хранятся в отдельной таблице `store_opening_bonuses` (supplier_id, store_label, amount, year).

## Прогон резолва имён 2026

49 имён в Excel против 89 поставщиков в Supabase (в State.md - 47 активных,
т.е. в справочнике есть неактивные/дубли, они шумят в кандидатах - проверить поле статуса).
Первый прогон: EXACT 5, REVIEW 41, NO_CANDIDATE 3.
Подтверждение - через `confirm_names.py` (интерактивно, пишет в `retro_fact_name_map`).

## Маппинг имён (решено)

Источник истины — ручной файл «Маппинг имен.xlsx» (колонки: имя в таблице ретро |
имя в HTML-панели | примечание). Заливается через `load_name_map.py --apply`.
Покрытие листа 2026: **49 из 49**, нерешённых нет. Автоугадывание больше не используется.

- 6 позиций → `is_ignored` («Не активный», «Оплата безнал»).
- 1 составная: «Бир Компани (Славутич пиво)» → БІР (Пиво Кег) + БІР (Пиво Славутич),
  разбивка сумм лежит в примечании к ячейке. Хранится в `split_targets` (jsonb), `needs_manual=true`.
- «Глобал Сервис (Немиров)» → `needs_manual=true`: в суммах зашиты компенсации акций
  без расшифровки, сверка почти всегда требует ручной правки.
- Часть поставщиков (Аванта-Трейд, Еліт Фуд, Ново-Баварський, Сервіс Про) **в наличной
  таблице отсутствует принципиально** — у них ретро по безналу/договору маркетинга,
  идёт отдельно. В сверке им нужен статус «не ожидается в этом источнике», а не «факт потерян».

## Примечания к ячейкам (красные треугольники)

Формат: `ASISTEC: 12.03.26` — автор и **дата, когда принесли деньги** (= поле «Дата оплаты»
в модалке админки). Найдено 289 шт. на листе 2026. Парсер — `parse_payment_notes.py`.

Две особенности:
- **Опечатки в годах**: `18.05.24` для периода 2026-04, `13.08.23` для 2026-07.
  Парсер помечает `date_warning`, автоматически НЕ исправляет.
- **Целевые суммы**: `из них 13000 стойки 15.06.26` — часть платежа не ретро,
  а компенсация (стойки/куби/акции). Соответствует полю «Доп. виплата (куби, акції)».
  Без выделения сверка даст ложное расхождение.

## Результаты аудита 2026 (январь-июль)

Колонка суммы расчёта в `retro_calculations` — **`total_retro`**.

Сводка: MATCH 202, CONFLICT 25, NOT_IMPORTED 51, FACT_ONLY 3, CALC_NO_FACT 11, NO_ROW_IN_EXCEL 38.

**Ключевое открытие.** 20 из 25 конфликтов — не ошибки, а два способа учёта одних денег:
Excel держит доплату в месяце фактического платежа, админка разносит её по месяцам начисления.
Признак: годовая сумма по поставщику в окне до последнего факта сходится в ноль.
Затронуты: Інтрейд Мікс (Батоша, Деліция), Арсенал ПК (Моршин), Глобал-Сервіс (Боржомі),
ТОВ Галиция, ТОВ Рідна марка. **Разноска в админке правильнее — Excel её затирать не должен.**

**Реальные расхождения (5):** Гранд Дистрибьюшн Схід −3 000 (06), Союз (Продукти) −2 972
(01: −1 708, 03: −1 264), Арсенал ПК (Азнаурі) −90, Продресурс ЛТД −10, Прометал −3.

**Не оплачено (расчёт есть, факта нет нигде):** 2026-07 — СТВ Схід (Якобз кофе-аппарты) 104 025,
Арсенал ПК (Шейк) 80 896, Роганський МК 73 111, Арсенал ПК (Моршин) 60 878, Рідна марка 27 676,
СТВ Схід (Якобз) 18 553, Союз (Продукти) 8 830, Глобал-Сервіс (Боржомі) 6 288, Бойчак ТД 1 934;
2026-06 — Рідна марка 26 958.

**Не импортировано (есть в Excel, нет в админке):** июль 28 ячеек на 767 406, июнь 7 на 22 603,
январь-май 16 нулевых ячеек.

## Принцип импорта (важно)

Админка — источник истины для уже внесённого. Excel — источник для нового.
`import_facts_to_db.py` по умолчанию пишет только отсутствующие ячейки.
Конфликты не перезаписываются без явного `--with-conflicts`; прежнее значение → `amount_previous`.


## Ревизия справочника правил (полный проход по rules_snapshot)

Просмотрены все 98 правил по 50 поставщикам. Найденное — ниже, в порядке денег/риска.
Статус: зафиксировано, не исправлено.

**Главное про диагностику.** `diagnose_retro.py` берёт приход из BQ по алиасам
поставщика БЕЗ фильтров расчёта (`sku_barcodes`, `excluded_sku_barcodes`,
`NOT REGEXP_CONTAINS(product_name, '^сырье')`, `income_source=torgsoft_ref`).
Поэтому «ПРИХОД НЕ СХОДИТСЯ» — артефакт, а не находка. Пока формула базы
не общая с `calculate_retro.py`, выводам диагностики верить нельзя.
Следствие: дельты Монжар −15 000 (Юмі Джелі исключены), Роганський МК −39 538,
ФКК Авангард −6 050 (оба кандидаты под фильтр «сырье»), Злагода −18 663
(все 3 правила SKU-ограничены) — объясняются фильтрами, а не потерей данных.
Арсенал (Шейк) считается от `torgsoft_ref`, с `incoming_transactions` не сверяется в принципе.

**Дефекты справочника:**
1. Союз (Оболонь) — нет ни одного алиаса 1С/BQ → приход 0 при ставках 20/25%.
   ОТЛОЖЕНО по решению пользователя, не трогать.
2. Укр-Крем-Опт, 01.01–15.03: двойной счёт. Правило 7% «Інший товар» без SKU-фильтра
   перекрывает правило 6% на 15 SKU круп. Лечится: крупы → excluded у правила 7%.
3. БІР кег: правило «Пиво Кег» только с 01.04, в Q1 7 исключённых SKU Славутича
   не покрыты ничем, хотя в Excel разбивка кег/бутылка есть с января.
4. Маршалл Табако: порог 10→8 SKU вписан в примечание одной строки правила
   (valid_from=2026-07). Пересчёт июля даст другое число. Нужна вторая строка с 01.08.
5. Ставка противоречит retro_raw: Баядера Q1 (5.0 vs 0.06), Продресурс янв-фев (5.0 vs 0.07).
   Юпітер: правило 12% с 01.02, примечание говорит «с марта» — февраль под вопросом.
6. Сервіс Про: valid_from=2026-05-01, а в примечании «исправлен на 2026-04-01». Апрель неясен.
   Плюс «Фрекен Бок» и «Салфетки» — два правила без SKU-фильтров, спасает только
   одинаковый процент; разойдутся ставки — будет двойное начисление.
7. Возвраты противоречат примечаниям: Юрія («без вирахування повернень» при returns_policy=subtract),
   СТВ Корона (returns_policy=none при документальных возвратах), Прометал.
8. Один алиас у двух поставщиков: «Інтрейд Мікс» (Батоша+Деліція),
   «БІР (Пиво)» (Кег+Славутич). Держится на SKU-фильтрах; опустеет список — приход задвоится.
9. Еліт Фуд: приход 0, алиас «ТОВ ЕлітФуд» слитно — проверить написание в BQ, потом уже INACTIVE.
10. Текст в числовых полях: retro_raw = «двадцать пять», «три», «Немиров-10%», «Капс Фуд-8%».
11. retro_base_type=payments у 7 поставщиков (АСК, Мон Шер, Хладопром, Дісна, Баядера,
    Продресурс, Рома) — расчёт зависит от залитой банковской выписки. Пустой месяц выписки
    = расчёт 0 = ложное «не оплачено». Нужна проверка покрытия `supplier_payments` перед сверкой.

## Эталонные таблицы BigQuery

Решение: подключить агрегатные эталоны по приходам и возвратам (без разрезов)
как независимый контроль. Назначение — не заменить пофактурный расчёт, а ловить
расхождения «наша база vs эталон» на уровне поставщик×месяц: если сумма по
`incoming_transactions` с фильтрами != эталон, значит потерян алиас, склад или тип документа.
Каталог таблиц снимается скриптом `bq_catalog.py`.


## Эталоны BigQuery (каталог снят, 81 таблица)

**Эталон приходов:** `torgsoft_incoming_ref_2026` — 58 217 строк, документный уровень.
Колонки: row_key, doc_date, doc_number, amount, currency, comment, supplier, store, amount_retail.
Только 2026. Сумма закупки — в `amount` (не amount_purchase, как в детальной).

**Эталон возвратов:** `torgsoft_outgoing_ref` — 8 205 строк, все годы.
Колонки: row_key, doc_date, doc_datetime, doc_number, store, supplier, company, currency,
amount_purchase, amount_national, amount_currency, amount_retail_fixed, amount_retail_now,
changed_by, **is_prosrok**, **is_internal**, source_file.
Флаги is_internal/is_prosrok в нашей `outgoing_to_supplier_transactions` отсутствуют —
возможно, во «возвраты поставщику» затесались внутренние перемещения и просрочка.

**Эталон продаж:** `torgsoft_sales_ref` — 1 765 568 строк. Уже используется
для правил с `income_source=torgsoft_ref` (Арсенал ПК Шейк).

**Ключевое ограничение: в эталонах нет barcode.** Сверка возможна только по полному
обороту поставщик×месяц без SKU-фильтров. Назначение — контроль СЛОЯ ДАННЫХ
(потерянный документ / алиас / склад), а не слоя правил.
Сверка — `reconcile_vs_etalon.py`, каталог — `bq_catalog.py` / `bq_catalog.csv`.

**Разница уровней:** эталон приходов документный (58 тыс.), наша `incoming_transactions`
построчная (1 035 095). Возвраты: эталон 8 205 док. против 47 115 строк.
Сравнивать только агрегаты и `COUNT(DISTINCT doc_number)`.

### Прочие справочники BQ, полезные проекту

- `supplier_mapping` (156) incoming_supplier → matrix_supplier — готовый источник
  недостающих алиасов, сейчас проектом не используется.
- `supplier_aliases` (14) — **своя таблица алиасов в BQ**, отдельная от одноимённой
  в Supabase. Два независимых источника алиасов, риск расхождения.
- `supplier_conditions` (105) — старый срез условий (retro_percent_min/max, retro_text,
  sponsor_fee, returns_percent...). **Третий источник истины по процентам** помимо
  retro_rules и Excel. Для сверки ставок пригодится, как рабочий — нет.
- `store_canon` (49) / `store_mapping` (43) — канонизация магазинов, нужна для
  исключения складов (актуально для правила Маршалл Табако по покрытию ТТ).
- `turnover_monthly` (1 343 668) — остатки/продажи по barcode×store×месяц,
  на ней считается покрытие ТТ для Маршалл Табако.
- Шум: ~20 таблиц вида *_backup_*, *_bak_*, transfer_pack_v5/v6 — в расчёте не участвуют.

## Отложено сознательно

- Союз (Оболонь), отсутствие алиасов — по решению пользователя не трогаем.


## Первая сверка с эталонами (2026)

**Эталоны покрывают не весь год.** `torgsoft_incoming_ref_2026` — нет сентября;
`torgsoft_outgoing_ref` — нет августа и сентября. Все строки отчёта вида
«эталон 0 / доков 0-N» — артефакт границы выгрузки, не расхождение.
Сверять только внутри покрытого окна (см. `bq_coverage.py`).

**Итог приходов:** эталон 261 801 307 vs наши 270 495 737. С поправкой на сентябрь
(~11 млн только у нас) реальная картина обратная: **эталон больше наших примерно на 2 млн**.

**Главная находка — теряются документы прихода.** У нас систематически меньше
`COUNT(DISTINCT doc_number)`, чем в эталоне:
Імператорська 08 (394/275, 63 594), Кока-Кола 07 (398/339, 127 996),
Кока-Кола 08 (352/346, 80 862), Союз Оболонь 08 (351/339, 448 304),
Арсенал Моршин 07 (177/168, 57 788), Арсенал Шейк 07 (165/155, 57 414),
Глобал-Сервіс Алкоголь 07 (8/5, 89 981), ДЛ Солюшн 08 (220/218, 88 019).
Видимая часть ~1,5 млн потерянного прихода ≈ 180 тыс. грн недоначисленного ретро.
Всего строк с расхождением >= 100: **222** (полный список в etalon_incoming_2026.csv).
Это дефект слоя данных, не правил. Приоритет выше, чем ревизия справочника.

**Полностью отсутствуют у нас:** Білосвіт, Хардпрод (есть в эталоне, нет в incoming_transactions).

**Возвраты:** `is_internal` бесполезен — суммы «всё» и «без internal» идентичны
(поле везде FALSE/NULL). `is_prosrok` отсекает 519 945 — выяснить, должна ли
просрочка уменьшать базу ретро; сейчас вычитаем.
Аномалии вне августа: Українська Злагода 01 (наши 27 999, эталон 0, 1 док),
СТВ Корона 03 (42 310, 1 док) — на расчёт не влияют (returns_policy=none).
Глобинський М'ясокомбінат 05: эталон 44 204 > наши 23 135 — потерян документ
возврата, база завышена, ретро переначислено.

**Баг в reconcile_vs_etalon.py:** списки only_e/only_o строятся по парам
поставщик-месяц, поэтому ловят границу выгрузки. Считать по годовым суммам.


### Точное окно эталонов (bq_coverage.py, прогон 11.09.2026)

| таблица | покрытие 2026 | строк |
|---|---|---|
| torgsoft_incoming_ref_2026 | 02.01 .. 31.08 (8 мес) | 58 217 |
| torgsoft_outgoing_ref | 01.01 .. 31.07 (7 мес) | 8 205 |
| incoming_transactions | 02.01 .. 09.09 (9 мес) | 438 976 |
| outgoing_to_supplier_transactions | 01.01 .. 31.08 (8 мес) | 19 990 |

Сверять приходы только 2026-01..2026-08, возвраты только 2026-01..2026-07.
Зашить отсечку в reconcile_vs_etalon.py, чтобы граница выгрузки не попадала в отчёт.

Термин: колонка «док» в отчёте = COUNT(DISTINCT doc_number) эталон/наши.
Недостача документов = недогруз накладных в incoming_transactions (дефект загрузчика),
в отличие от расхождения сумм при равном числе документов (цены/скидки).


## Поправка: «потеря документов» была ошибкой диагноза

Сравнение эталона с нашими данными по `doc_number` даёт ложные срабатывания.
`torgsoft_incoming_ref_2026` хранит одну поставку ДВАЖДЫ — под коротким внутренним
номером и под длинным внешним. Пример (Імператорська 08): эталон «2026119251 09.08 1314»
и «184 09.08 1314» — одна накладная. Совпадает дата и сумма, вся сотня строк парная.
Правильный метод: гасить несовпавшие документы парой (дата+сумма) среди уже совпавших,
и только остаток считать дырой. Реализовано в `find_bq_gaps.py`.

**Реальная дыра подтверждена:** 29–30.07.2026 загрузка оборвалась.
Глобал-Сервіс (Алкоголь) — документ 85515 от 29.07 на 89 981,34 отсутствует целиком
(ретро 10–15% -> ~13 тыс. грн недоначислено). У Кока-Колы пропуски тех же двух дней.
Дата общая для разных поставщиков -> сбой загрузчика, а не фильтр.

## Чего не хватает в BigQuery для корректного расчёта

1. Дозалить оборванные дни приходов (мин. 29–30.07; полный список — `find_bq_gaps.py`).
2. Флаги `is_internal` / `is_prosrok` в `outgoing_to_supplier_transactions` —
   в эталоне есть, у нас нет. is_prosrok = 520 тыс./год. Если просрочка не должна
   уменьшать базу — сейчас занижаем ретро всем с returns_policy=subtract.
3. **Оплат поставщикам в BQ нет вообще.** 7 поставщиков с retro_base_type=payments
   (АСК, Мон Шер, Хладопром, Дісна, Баядера, Продресурс, Рома) зависят только от
   supplier_payments в Supabase. Не залита выписка -> расчёт молча даёт 0.
4. Эталоны обрываются: приходы 31.08, возвраты 31.07. Август-сентябрь сверять нечем.
5. Признак сырья — сейчас regexp `^сырье` по product_name, хрупко. Нужен флаг.


## Дыры в приходах: итог (find_bq_gaps.py, окно 2026-01..08)

Дубли нумерации эталона погашены: 164 954,55 — метод сравнения (дата+сумма) рабочий.
Реальные дыры: **117 документов на 497 004,27**.

**Сбой загрузки 29–30.07.2026** — 438 006 (88%), 105 документов.
Остальные 11 дат — по одному документу, природа иная, разбирать отдельно.
Крупнейший одиночный: Монжар 17 934,84 от 27.07 (весь его вклад одним док.).

**Оценка потерь ретро — НЕ 12% плоско.** Не влияют на ретро вовсе:
Арсенал ПК (Шейк) 56 298 (income_source=torgsoft_ref), Хладопром 16 926 и Рома 7 049
(retro_base_type=payments), СТВ Якобз кофе-аппараты 6 345 (per_portion_sold) — итого 86 618.
Союз (Оболонь) 90 187 — не трогаем по решению пользователя (~20 тыс. при 20-25%).
По фактическим ставкам остальное: Глобал-Сервіс Алкоголь ~14 000, Арсенал Моршин 4 623,
СТВ Корона 3 583, БІР Кег 3 447, Авангард ~3 000, Монжар 2 690, Виналь 2 302 (с ×0.8),
Рідна марка 1 152, Юпітер 920, Боржомі 479.
**Итого ~36 тыс. грн недоначисленного ретро**, из них ~30 тыс. лечится перезаливкой 29–30.07.

Вывод: при оценке ущерба от дыр в incoming_transactions обязательно фильтровать
поставщиков по retro_base_type / income_source — иначе завышение вдвое.


## Правило окна сверки

Ретро считается по ПОЛНОМУ прошедшему месяцу. Текущий (незакрытый) месяц из всех
сверок исключать — иначе даёт ложные «эталон 0 / у нас N» на всю выгрузку.
На 11.09.2026 рабочее окно = 2026-01..2026-08.
Дефолт `--to` в reconcile_vs_etalon.py / find_bq_gaps.py = последний закрытый месяц.

## Чего не хватает в данных (уточнено)

1. **Приходы 29–30.07.2026** — сбой загрузки, 105 док. на 438 006 (~30 тыс. грн ретро). Перезалить.
2. **Эталон возвратов обрывается 31.07**, а август — уже закрытый расчётный месяц.
   Продлить `torgsoft_outgoing_ref` минимум по 31.08, иначе августовские возвраты
   не с чем сверять.
3. Флаги `is_internal` / `is_prosrok` в `outgoing_to_supplier_transactions` (есть в эталоне).
4. Источник оплат для retro_base_type=payments (7 поставщиков) — сейчас только
   supplier_payments в Supabase из банковской выписки.
5. Сентябрь грузить/сверять не нужно — месяц не закрыт.

12 одиночных дыр вне 29-30.07 (Авангард 24 811 от 11.05, Монжар 17 935 от 27.07,
Оболонь 9 105 от 18.08, СТВ Бащинський 2 065 от 31.03 и мелочь) проверяются
скриптом `probe_gap_docs.py`: ищет документ по номеру где угодно и по сумме
в окне ±7 дней у любого поставщика. Различает «нет вовсе» / «другой алиас» / «сдвиг даты».
Дата 31.03 у СТВ — типичная граница месяца, вероятен сдвиг в апрель.


## Классификация дыр: две ошибки метода исправлены

1. Сравнение шло ВНУТРИ одинаковой строки `supplier`. Разные написания алиаса
   одного поставщика («Авангард Дистрибуції (ваговий)» vs «Авангард Дистрибуції»,
   «Союз (Продукти, МакМай, Ямуна)» vs «Союз (Продукти)») давали ложные дыры.
   Сводить к каноническому поставщику через supplier_aliases.
2. Совпадение по `doc_number` принималось без проверки суммы. Номера не уникальны
   между поставщиками: Монжар 1084 «нашёлся» у Юрії (824,22), БІР 9073 — у Продбази
   в сентябре, Юрія 2451 — у СТВ Данон в январе. Номер засчитывать только при
   совпадении суммы (±0,01).
   => вердикт «ЕСТЬ ПО НОМЕРУ» (30 док, 139 777) недостоверен, пересчитан
   скриптом `classify_gaps.py`.

Подтверждённые ложные тревоги: Авангард 883695 (11.05, 24 810,84) и
Союз Продукти 60136 (21.05, 1 277,10) — документы на месте.

**Новый класс ошибки: приход отнесён не к тому бренду.** Док 94736 от 08.08 на 533,64
в эталоне — Оболонь, у нас — «Союз (Наш Сік)». Дата и сумма совпадают.
Ставки разные (20/25% против 12%), деньги тихо перетекают между правилами,
сверка по суммам такое не ловит. Проверить по всем мультибрендовым поставщикам
(Союз, СТВ, Арсенал, Глобал-Сервіс, Інтрейд, БІР).

Подтверждённые MISSING среди одиночек: Монжар 17 935 (27.07), Оболонь 9 105 (18.08)
и 651 (03.06), Рідна марка 1 019 (28.07).
СТВ Бащінський 31.03 -> 26.03 — сдвиг внутри месяца, на расчёт не влияет.

## Перезалив 29-30.07: результат (classify_gaps после перезалива)

Было 117 дыр: MISSING 104 (457 070,43). Стало: OK_MATCH 103 (462 775,15),
DATE_SHIFT 6 (20 459,98), MISSING 7 (13 235,50), WRONG_BRAND 1 (533,64).
Дыра 29-30.07 закрыта.

ВНИМАНИЕ: документов стало 60 084 против 59 798 (+286), а закрыться должно было ~103.
Остаток ~180 документов не объяснён -> проверка `check_reload_dups.py`
(двойная заливка по DISTINCT loaded_at, дубли по incoming_id+line_number,
перебор против эталона за 29-30.07). Задвоенный приход завышает базу ретро,
это важнее остатка дыр.

### Ошибка метода в cross-supplier матчинге (исправить в classify_gaps.py)
WRONG_SUPPLIER выносился по совпадению ОДНОЙ СУММЫ в окне +-7 дней у другого
поставщика, при разных doc_number. Все 3 таких вердикта были датированы 30.07,
т.е. это были документы июльской дыры со случайно совпавшей круглой суммой
(2 076,00 / 677,04 / 492,48 - кратны упаковке, коллизии на 60 тыс. документов
неизбежны). После перезалива все 3 ушли. Правило: межпоставщикский вердикт
только при совпадении doc_number И суммы; совпадение одной суммы у другого
поставщика = MISSING.

### Остаток после перезалива
- MISSING 7 док / 13 235,50, из них Оболонь 9 104,62 от 18.08 - НЕ ТРОГАЕМ
  (ограничение пользователя). Прочие 6 док ~4 131 (~500 ₴ ретро) - закрыть допуском.
- DATE_SHIFT вырос 5 -> 6 док и 8 802 -> 20 460: похоже, Монжар 17 935 от 27.07
  не потерян, а лежит на другой дате. Проверить по gaps_classified.csv,
  совпадает ли МЕСЯЦ: внутри месяца - безразлично, через границу - переносит бонус.
- WRONG_BRAND: Союз (Оболонь) 533,64 от 08.08 отнесён к бренду Союз (Наш Сік).
  Единственный подтверждённый кейс неверного бренда. Ставки 20-25% vs 12%.
  Нужен отдельный прогон по мультибрендовым поставщикам (Союз, СТВ, Арсенал,
  Глобал-Сервіс, БІР) - систематика или единичный случай.

## Окно сверки в reconcile_vs_etalon.py

Флага --returns НЕТ: приходы и возвраты считаются в одном прогоне.
Добавлены --from-period / --to-period (по умолчанию 2026-01..2026-08).
Причина: приходы у нас за 9 месяцев, эталон - 8; сентябрь давал ложную дельту.
Эталон возвратов продлён до 31.08 - базу возвратов (всё подряд / без is_internal /
без internal+prosrok) выбирать заново на полном окне.

## Бэклог
1. Источник оплат для retro_base_type=payments (7 поставщиков).
2. Явный флаг is_raw вместо regex по product_name.
3. Прогон по мультибрендовым поставщикам на ошибки бренда.
4. Долги по коду: хардкод Decimal("0.80") в 4 местах вместо VAT_FACTOR;
   таблица retro_adjustments; защита периодов approved/paid (+ --force).

## Проверка на дубли после перезалива: дублей НЕТ

Мой тест по (incoming_id, line_number) был НЕВАЛИДЕН: PK таблицы - line_id,
одна строка документа лежит в разных store => 3 880 "пар" и 2,3M "лишней суммы" - артефакт.
Две партии loaded_at на документ - тоже не доказательство (доливка частями).
Доказательство обратного: Jan-Aug наши 261 235 220,88 vs эталон 261 801 307,30,
т.е. МЕНЬШЕ на 566 086,42. Задвоение дало бы только перебор.
Пример: Союз (Оболонь) 05 - наши 6 148 375 < эталон 6 221 188, хотя док 63938
на 147 тыс. числится в двух партиях.
Правило: дубли проверять ТОЛЬКО по line_id и по сумме документа против эталона.

## Структура остаточной дельты (Jan-Aug, эталон - наши = +566 086)

Дельта ДВУНАПРАВЛЕННАЯ, значит это не потеря загрузки, а минимум три разных причины:

1. ПЕРЕИМЕНОВАНИЯ ПОСТАВЩИКОВ -> зеркальные пары, ложные дыры.
   Кріофудтрейд -7 097,40 / Айсмейстер +7 097,40 (29-30.07, один документ).
   Грілайн ЛТД +9 900 / Геотрейд-ЮА -9 900 (05 и 07).
   Кандидаты: Білосвіт, Хардпрод (есть в эталоне, нет у нас), Геотрейд-ЮА (наоборот).
   ПРИЧИНА: reconcile_vs_etalon.py джойнит по СЫРОЙ строке supplier.
   Та же ошибка метода, что исправлена в classify_gaps.py. Джойнить по каноническому
   поставщику через supplier_aliases.

2. АВГУСТ НЕДОЗАЛИТ (главный реальный дефект): -921 766, док 7081 vs 7279.
   Імператорська 275 док vs 394 (-63 594), Союз (Оболонь) 339/351 (-448 304),
   ДЛ Солюшн -88 019, Кока-Кола -80 862, Хладопром -42 489, Роганський -27 641.
   Похоже на обрез по дате в конце августа -> смотреть по дням.

3. ИЮЛЬ С ПЕРЕБОРОМ: +375 335, док 7721 vs 7667.
   БІР (Пиво) 457/451 (+79 263), Моршин 182/177 (+44 611), Шейк 171/165 (+40 351),
   Авангард 171/162 (+29 436), Мінеральні Води 165/155 (+35 582), Гама 325/304.
   У НАС БОЛЬШЕ документов, чем в эталоне -> либо эталон неполон, либо сдвиг
   даты через границу месяца. Проверяется по дням (probe_delta_structure.py, блок C).

Инструмент: probe_delta_structure.py - [A] мультипартийные док vs эталон,
[B] объяснение неуникальности incoming_id+line_number, [C] дельта по дням июль-август,
[D] кандидаты в переименования (док+дата+сумма совпали, поставщик разный).

## ВОЗВРАТЫ: вопрос флагов закрыт

Эталон на полном окне: всё подряд 3 742 934,92 | без is_internal 3 742 934,92
| без internal+prosrok 3 150 812,28. Наши 3 757 849,01.
- is_internal ПУСТОЙ (суммы идентичны) -> в модель не тащить.
- is_prosrok снимает 592 122,64; наши данные просрочку ВКЛЮЧАЮТ.
  Это вопрос СЛОЯ ПРАВИЛ (уменьшает ли просрочка базу), а не слоя данных.
- База зафиксирована: "всё подряд". Дельта -14 914,09 на 3,7M = 0,4%, в допуске.
Материальные расхождения (4): Алко Трейдінг 08 (45 493, док 1/0),
СТВ Схід Корона 03 (42 310, 0/1), Українська Злагода 01 (27 999, 0/1),
Глобинський 05 (21 069). Паттерн 1/0 и 0/1 => проверить как алиасы, а не дыры.

## ФИНИШ сверки слоя данных (Jan-Aug 2026)

Дельта 566 086 на 261,8M = 0,22%. Слой данных ПРИНЯТ, сверку закрываем.
Допуск сверки: 0,5% или 5 000 ₴ на пару поставщик-месяц.

- Дубли: ОПРОВЕРГНУТЫ. Строк 440 888 = уникальных line_id 440 888, дублей по PK 0.
  424 мультипартийных документа дают дельту В ОБЕ СТОРОНЫ (Кока-Кола 89386 -61 289,
  БІР 11949 +37 264) - доливка частями и правки строк, не задвоение.
  incoming_id+line_number НЕ ключ: 2787 пар с разными store, 3214 с разными barcode.
- Август: обреза по дате НЕТ (меньше документов ровным слоем во все дни).
  Отсутствует подмножество поставщиков. Главный: Імператорська 275 док vs 394 (-63 594).
- Июль: перебор у нас - неполнота ЭТАЛОНА, не наша (20.07: эталон 58 док vs наши 123).
- Возвраты: база "всё подряд", is_internal пустой, is_prosrok - вопрос правил.

### Неверная привязка поставщика (ставки перекошены) - ИСПРАВИТЬ
Не дыры, а приход, ушедший чужому поставщику. Разные юрлица со схожими именами:
  Хардпрод 11 262,90 / 12.08 / у нас Хладопром
  Білосвіт 18 828,50 / 14.08 / у нас Бісквіт і Ко
  Кріофудтрейд 7 097,40 / 29.07 / у нас Айсмейстер
  СТВ (Корона) 4 909,14 / 31.07 / у нас СТВ (Каваапарат)
  Авангард (ваговий) 24 810,84 / 11.05 / у нас Авангард Дистрибуції -> АЛИАС,
    закрывает последнюю крупную "дыру" из списка 117
  Союз (Оболонь) <-> Союз (Наш Сік): 3 574,56 и 3 011,28 встречно - НЕ ТРОГАЕМ
  Ново-Баварський 428,10 / 21.07 / у нас Юрія; Гама/Магазин 0,00 - мелочь

### Незакрытый остаток, принят как допустимый
MISSING 7 док / 13 235,50 (из них Оболонь 9 104,62 от 18.08 - не трогаем).
DATE_SHIFT 6 док / 20 459,98 - проверить только совпадение МЕСЯЦА.
Оценка недоначисления ретро по всему остатку: ~36 тыс. ₴.

## Возврат к цели проекта
Сверка BQ была отвлечением. Возвращаемся на этап 2-3: резолв имён
(retro_fact_name_map, только EXACT) и запись фактов в retro_payments_fact.

## СТОП-ПРАВИЛО: не склеивать поставщиков и бренды алиасами

Совпадение «doc_number + дата + сумма» у двух РАЗНЫХ имён означает только,
что документ один. Это НЕ основание для алиаса. Алиас добавляется ТОЛЬКО когда
это доказанно одно юрлицо и один бренд с одной ставкой.

Разбор блока [D] probe_delta_structure.py — правильная трактовка:

1. ОДИН ПОСТАВЩИК, РАЗНЫЕ БРЕНДЫ, РАЗНЫЕ УСЛОВИЯ — алиас ЗАПРЕЩЁН,
   это ошибка привязки БРЕНДА на документе:
     Союз (Оболонь) <-> Союз (Наш Сік)   3 574,56 и 3 011,28   НЕ ТРОГАЕМ (Оболонь)
     СТВ (Корона) <-> СТВ (Каваапарат)   4 909,14              один дистрибьютор,
       условия совершенно разные -> кофейное оборудование != кондитерка
2. РАЗНЫЕ ЮРЛИЦА со схожими именами — алиас ЗАПРЕЩЁН. Если поставщика нет
   в справочнике, его надо ЗАВЕСТИ со своими правилами:
     Хардпрод 11 262,90 / 12.08   != Хладопром
     Білосвіт 18 828,50 / 14.08   != Бісквіт і Ко
     Кріофудтрейд 7 097,40 / 29.07 != Айсмейстер
     Ново-Баварський 428,10 / 21.07 != Юрія
3. ТРЕБУЕТ ЧЕЛОВЕКА: Авангард Дистрибуції (ваговий) 24 810,84 / 11.05.
   Весовой может иметь отдельную ставку -> сверить retro_rules,
   и только если ставка одна - считать алиасом.

Ни одна из этих правок не делается автоматом. Никакого FIRST_WORD, никакого
матчинга по сумме. Либо EXACT и одна ставка, либо человек.

## Честная переоценка дельты сверки (поправка)

566 086 - это НЕТТО, плюсы и минусы погасились. Для ретро гашение не работает:
бонус считается по каждой паре поставщик-месяц отдельно. Считать сумму МОДУЛЕЙ.
По видимым 40 строкам ~1,5M, всего строк 153 => валовая дельта ~1,7-1,8M (~0,7%),
это 200+ тыс. ₴ ретро, а не 36 тыс. Прежняя оценка 0,22% была занижена.

ПРАВИЛЬНАЯ МЕТРИКА (ещё не мерили): дельта только по поставщикам с активными
retro_rules и только по их брендовым SKU. Крупнейшие куски валовой дельты -
Імператорська, Кока-Кола (ретро НЕ платится) и ДЛ Солюшн (88 тыс. на обороте 10M = 0,8%).
Посчитать один раз - и закрыть тему числом.

## Где мы на самом деле (этапы из раздела «Этапы (v1)»)

Сверка BQ была подготовкой данных для этапа 4, а не этапом.
Код: ~2,5 из 5. Данные: ~1,5 из 5.
1. Парсер Excel - ГОТОВО (10 контрольных сумм сошлись).
2. Резолв имён - код есть (resolve_supplier_names.py), НЕ доведён:
   таблицы retro_fact_name_map в Supabase нет, подтверждённых сопоставлений 0.
3. Запись фактов - НЕТ. 4. Автосверка - НЕТ. 5. Диагностика - НЕТ.

## ТОНКОЕ МЕСТО №1 (решить до записи фактов!)

Строки Excel - это НЕ поставщики, а НАПРАВЛЕНИЯ: «Арсенал (Моршин)»,
«Арсенал (Азнаурі)», «Союз (Оболонь)», «Союз (Наш Сік)».
Одна ячейка = факт по паре ПОСТАВЩИК+БРЕНД.
retro_payments_fact привязан к supplier_id => три строки Арсенала схлопнутся
в одну сумму, разные ставки внутри поставщика станут неразличимы.
Это ровно проблема «Корона vs Каваапарат».
РЕШЕНИЕ: retro_fact_name_map резолвит в пару (supplier_id, supplier_brand_id).
Переделывать после заливки данных будет дорого.

## Прочие тонкие места

2. НДС: VAT_FACTOR не используется, хардкод Decimal("0.80") в 4 местах,
   не решено x0,8 или /1,2 (разница 4,2%). До решения каждый поставщик с вычетом
   НДС даёт ложное расхождение ровно на 4,2%, диагностика будет искать призраков.
3. Уровень сравнения: Excel = 1 сумма на месяц, расчёт = строки по брендам.
   Сверять на АГРЕГАТЕ поставщик-месяц, при расхождении спускаться в бренды.
4. None vs 0 обязаны дойти до БД разными: 0 = «платить нечего» (проверить, что
   расчёт тоже 0), NULL = «не заполнено» (молча пропустить, не считать неоплатой).
5. Колонка «Условия» - человеческая заметка, источник истины = retro_rules.
   Но использовать как СИГНАЛ: Excel 15% vs правило 12% => кандидат в расхождение
   ставки (уже находили у Баядеры, Продресурсу, Юпітера).
6. sb_get/sb_post в 8 файлах, автопагинация только в calculate_retro.py ->
   остальные молча режут на 1000 строк. Перевести всех на sb.py ДО того,
   как сверка начнёт читать многотысячные retro_calculation_details.
7. Идемпотентность: повторная загрузка того же файла не должна плодить дубли
   (натуральный ключ + история предыдущего значения); периоды approved/paid
   не перезаписывать без --force; ручные правки - в retro_adjustments поверх расчёта.
8. Дрейф округления при пропорциональном распределении возвратов -> метод
   наибольшего остатка (остаток последнему правилу).

## Порядок работ до цели «загрузил файл - дальше само»

Ш1. DDL retro_fact_name_map (ключ: excel_name_normalized -> supplier_id +
    supplier_brand_id + is_ignored) + прогон резолвера + ручное подтверждение CSV
    + apply_name_map.py заливает решения. Один раз, дальше имена узнаются сами.
Ш2. write_retro_facts.py -> retro_payments_fact, идемпотентный upsert.
    После этого суммы появляются в админке сами (admin.html читает Supabase напрямую).
Ш3. reconcile_facts.py -> retro_reconciliation, два порога (>=1 ₴ и >=0,5%).
Ш4. Диагностика: детерминированная лестница гипотез, каждая возвращает ЧИСЛО,
    побеждает первая, закрывающая разрыв в пределах допуска. Порядок - от дешёвых
    к дорогим: окно данных/закрытость месяца -> форма НДС -> ставка и valid_from ->
    политика возвратов -> привязка алиаса/бренда -> SKU-фильтры -> дыра в слое данных.
Ш5. Кнопка загрузки Excel в админке вместо запуска из PowerShell.

Отложено сознательно: нормализация имён и исключения по Excel - отдельным этапом
после Ш1, чтобы не смешивать с резолвом.

## Маппинг имён: ЗАКРЫТО (load_name_map.py --apply)

Источник истины - «Маппинг имен.xlsx» (левая колонка = лист ретро, правая = админка).
49 строк, 6 ignore («Не активный», «Оплата безнал»), нерешённых имён нет.
Правые имена совпали с suppliers.name напрямую: НАПРАВЛЕНИЯ уже отдельные
строки в suppliers («Арсенал ПК (Шейк)», «СТВ Схід (Якобз кофе-аппарты)»).
=> Тонкое место №1 СНЯТО: пара поставщик+бренд в маппинге НЕ нужна,
Корона и Каваапарат разведены на уровне поставщика.
Свой велосипед apply_name_map.py удалён - дублировал load_name_map.py.

### Расхождение двух файлов (лист vs маппинг) - поправить в Excel
  в листе есть, в маппинге нет: «грин дей (виналь)» (в маппинге «грин дей (бест бренд)»),
                                «ддс+» (вообще не внесён)
  в маппинге есть, в листе нет: «жирнов», «маршал табакко» (в листе иное написание)
Держать оба файла синхронными; кросс-проверка load_name_map --facts это ловит.

## Примечания к ячейкам: разбор (dump_cell_comments -> parse_note_components)

126 примечаний: 115 - только дата внесения денег (ASISTEC: 12.03.26),
11 - с составом. Платежей, затрагивающих другие периоды, НЕТ (доплат/перерасчётов нет).

ИСПРАВЛЕН БАГ ПАРСЕРА: если число в примечании РАВНО сумме ячейки - это
заявленный итог, а не слагаемое; остальные метки - что внутри этого итога.
Было ложное расхождение E8 БІР «204217 5400дмп» +5 400.
Отличать от честного слагаемого: C8/F8 «263756 + 5400 дмп» = 269 156 (сходится).

### Что примечания дают для сверки
- Состав суммы: Арсенал «азнаури + зубровка», СТВ «кубы» (9 900/10 200 - кофе-кубы),
  БІР «дмп» 5 400 - разные направления внутри одной ячейки.
- Дата внесения денег - объясняет тайминг: оплата за месяц приходит в следующем
  (янв -> 27.02, фев -> 31.03). Для retro_base_type=payments это и есть причина
  расхождений ±0,2-0,9%, которые ранее списывали на «тайминг».
- F30 Ресурс «5%-9270- 2%» - СИГНАЛ конфликта ставки (5% vs 2%), сверить с retro_rules.

### Требует решения человеком (2)
1. F4 Арсенал (Азнаурі) 2026-03: ячейка 60 872, «азнаури 60782 + зубровка 3090»
   = 63 872. Похоже на опечатку в примечании (60782 vs 60872). Проверить.
2. SPLIT: «Бир Компани (Славутич пиво)» -> БІР (Пиво Кег) + БІР (Пиво Славутич).
   Одна ячейка = два поставщика, пропорция из примечания НЕ выводится
   (там «дмп», а не разбивка по кегам). Правило на первое время: сверять СУММОЙ
   по двум поставщикам вместе, needs_manual=true, точную разбивку вносит человек.

## Сессия 11.09.2026 (вторая): шаг 3, BQ, реорганизация

### Структура папки (с 11.09)
Скрипты конвейера - `core\` (запуск из корня: `python core\<скрипт>.py`), разовые миграции - `tools\`,
разборы по поставщикам - `analysis\`, probe - `_archive\`. Выгрузки «Ретро Бонусы» - `Ретро_Excel\`
(старые версии - `Ретро_Excel\старые\`), справочники - `data\справочники\`. Все генерируемые файлы
`core\` пишут в `output\` (через `core\paths.py`). admin.html и .bat остаются в корне.
Ярлык «Расчет_ретро.lnk» указывал на несуществующий run_retro.bat - создан, открывает меню.

### ВАЖНО: какой Excel актуален
В корне лежала копия от 15.05 (только янв-апр, итоги другие). Актуальная - выгрузка в Downloads
(«Ретро Бонусы_9.xlsx» 11.09 12:06), скопирована как `Ретро_Excel\Ретро Бонусы 2026-09-11.xlsx`.
Все 10 контрольных сумм сошлись. Рассинхрон «грин дей / ддс+» в этой версии снят (без маппинга 0).
F4 Азнаурі в новом файле = 63 872 (опечатку исправили), против админки 63 962 -> конфликт −90.

### Шаг 3: сухой прогон import_facts_to_db.py (запись НЕ выполнена, ждёт пользователя)
Новых 54 (янв-май 14 нулей; 06: 7 шт / 22 603; 07: 28 / 767 406; 08: 3 / 41 309), совпадает 202,
конфликтов 25 (не пишутся), разовых 8, ignore 6, составных 1, без маппинга 0. Оболони среди новых нет.
Исправлено: EPS 0,005 -> 1 ₴ (ложный конфликт Союз Золоте Зерно 04); `amount_previous` во всех
строках (PostgREST отвергает батч с разными ключами - ломался --with-conflicts); dry-run печатает каждую запись.
Проверено: колонки retro_payments_fact / store_opening_bonuses есть; уникальные индексы под оба
on_conflict есть (пустой upsert; контроль на заведомо неверном ключе даёт 42P10).
retro_reconciliation и retro_adjustments в БД НЕТ.

### BQ: сверка по ретро-скоупу (probe_retro_scope_bq.py) - та самая «ещё не мерянная» метрика
Группа = поставщики, связанные общим алиасом; в скоуп - месяцы с активным правилом от прихода.
- Целостность: incoming 440 888 строк = 440 888 line_id; outgoing 19 990 = 19 990 полных строк. Эталон возвратов до 31.08.
- ПРИХОДЫ, скоуп (264 пары): эталон 74,27M / наши 74,45M, нетто −190 тыс., ВАЛОВАЯ 477 тыс. (0,64%),
  ≈ 62 тыс. ₴ ретро. Крупное: БІР 07 −79 тыс. (неполнота эталона), БІР 08 +33 тыс.,
  Авангард у нас БОЛЬШЕ эталона 5 месяцев подряд (систематика, не дыры), Азнаурі 06 +18 тыс. (док 7/5).
  Вне скоупа валовая 808 тыс., Оболонь 567 тыс. (не трогаем).
- ВОЗВРАТЫ, скоуп: валовая 4 253 ₴ (≈ 411 ₴ ретро). Чисто.
- Не ретро (решение пользователя 11.09, НЕ заводить и не алиасить): ПП Торговий Дім СМК Груп,
  Айсмейстер, Інтрейд Мікс (Чай), Авангард Дистрибуції (продукти), Грілайн ЛТД, Геотрейд-ЮА.

### НЕ-эталон: сохранённые расчёты УСТАРЕЛИ (главная находка для этапа 4)
loaded_at: приходы 01,03-06 перезалиты 17.07 (расчёты 16.07), 07 - 11.09 (расчёт 06.08), 08 - 07.09;
возвраты 01-07 перезалиты 17.08. Пересчёт `--no-save` на текущих данных (probe_recalc_vs_saved.py):
сохранено 13 692 933 (без доп. выплат) vs сейчас 13 812 652, нетто +119 719, валовая 139 727, 70 пар.
Июль +62 тыс.: Монжар 0 -> 12 816 (док 17 935 от 27.07 догружен -> пробит порог 80 тыс.; факт Excel 12 650),
Глобал-Сервіс Алкоголь 22 868 -> 35 890 (док 85515 от 29.07), БІР Славутич +12 422, Моршин +8 192.
Approved-расхождения (Глобал-Сервіс Алк 01 +12 037, Укр-Крем-Опт 01-03 - двойной счёт 7%+6%) пересчёт НЕ трогает.
=> Сверять факт с расчётом можно только после пересчёта. Метод «loaded_at > created_at» сам по себе
неверен (перезалив целиком), верен только пересчёт.
Доп. выплаты из админки лежат строками в retro_calculation_details без правила (7 шт, 53 772 ₴:
кубы СТВ, ДМП БІР 05 5 400 в статусе completed, компенсация акции ГС) - пересчёт с сохранением
СОТРЁТ те, что не approved. Сначала retro_adjustments.

## РЕШЕНИЯ ПОЛЬЗОВАТЕЛЯ 11.09.2026 (закрывают вопросы ревизии справочника)

ПЕРЕД ЛЮБЫМ ВОПРОСОМ ПО ПРАВИЛАМ - читать output\rules_snapshot.md (обновляется `python core\dump_rules.py`)
и сверять с фактом по числам. Пользователь не любит вопросы, ответ на которые есть в БД.

1. НДС: «минус 20%» = ×0.80 для всех subtract_vat_from_retro. Хардкод убран, `apply_vat()` в calculate_retro.py
   (регрессия: Виналь 07 = 26 959,82 до и после).
2. Ставки Баядера Q1 / Продресурс янв-фев / Юпітер 12% с 01.02 / Сервіс Про (нал до 30.04, Ф1 с 01.05)
   / Укр-Крем-Опт (7%+6% до 15.03) - НЕ ТРОГАТЬ: до августа факт сходился. Пункты 2, 5, 6 ревизии закрыты.
3. ЯНВАРЬ-ИЮНЬ НЕ ПЕРЕСЧИТЫВАТЬ. Проверено числами (probe_fact_vs_calc_scope.py): сохранённый расчёт
   = факт в 11 парах, где пересчёт на перезалитых данных уводит (Виналь 01/04, Укр-Крем-Опт 01-03, Рома 03,
   СТВ Якобз 03-04, Монжар 04); пересчёт нигде не даёт совпадения, которого не было (кроме Корма 05 +724).
   Перезаливы 17.07 / 17.08 немного сдвинули приход - истина для янв-июн = сохранённый расчёт.
   ИЮЛЬ пересчитывать: даёт совпадения Монжар (−166), Виналь (0), Юпітер (0), Авангард (−7),
   СТВ Корона (+137), Глобал-Сервіс Алк (+16 260 -> +3 238), БІР (+9 276 -> −4 660).
   Пересчитан 11.09 только по 21 поставщику с разницей (+ Маршалл), Оболонь не затронута.
4. Маршалл Табако: порог 10 SKU (8 ставился подгонкой). Правило исправлено 11.09 (min_sku_per_store=10).
   Август не меняется: все 13 ТТ имеют ровно 10 SKU = 10 400 = факт. Июль: 4 ТТ -> 2 ТТ (3 200 -> 1 600).
5. СТВ Корона returns_policy=none - ВЕРНО (физ. обмен). Юрія returns_policy=subtract - ВЕРНО
   (факт сходится янв-июл в пределах −293..+241). Текст «без вирахування повернень» в notes Юрії устарел.
6. Авангард Дистрибуції (ваговий) - ретро НЕТ (алиас excluded, так и оставить).
7. Ново-Баварський - ретро по безналу (Ф1), в Excel (только наличка) его не будет никогда; сверять только
   в ретро-панели. Статус сверки «не ожидается в Excel». Так же Аванта-Трейд, Еліт Фуд, Сервіс Про с 05.
   Внимание: bonus_form НЕ годится как признак безнала - у Жирнова marketing_service, но в notes «наличка»
   и строка в Excel есть.
8. Не ретро (не заводить, не алиасить): ПП ТД СМК Груп, Айсмейстер, Інтрейд Мікс (Чай),
   Авангард Дистрибуції (продукти), Грілайн ЛТД, Геотрейд-ЮА.

## Следующий шаг
1. ГОТОВО 11.09: retro_payments_fact +54 (import_source=excel_retro_sheet, всего 304), повторный прогон
   даёт «новых 0 / совпадает 256» - идемпотентно. store_opening_bonuses: 8 шт на 22 008.
   Первый --apply упал на store_opening_bonuses (PGRST204: нет колонки payment_date) - моя ошибка
   проверки схемы (пустой upsert не ловит лишние ключи). Ключ убран, перезапуск записал только 8 бонусов.
   НЕ записаны 50 000: D8/I8 «Бир Компани (Славутич пиво)» роган.148 и зерновая 6/5 по 25 000 -
   строка составная (Кег + Славутич), поставщика выбирает человек. Скрипт теперь выводит такие ячейки.
2. retro_adjustments (DDL в SQL-редакторе Supabase) + перенос 7 доп. выплат - защита на будущее;
   для июля не блокирует (доп. выплат в июле нет).
3. Автосверка -> retro_reconciliation. Истина янв-июн = сохранённый расчёт; перед сравнением проверка
   свежести расчёта только для незакрытых месяцев.
